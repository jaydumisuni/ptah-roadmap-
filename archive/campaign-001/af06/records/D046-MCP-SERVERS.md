# D046 — Model Context Protocol servers

Status: candidate record — paired evidence complete

Primary Archivist: `AF06-P03`

Independent Verifier: `AF06-V03`

Inspected: 2026-07-23

## Canonical source identity

- source: `modelcontextprotocol/servers`;
- default branch: `main`;
- exact inspected commit: `d31124c982401739917fd817c2a59db344529c16`;
- root licence: MIT;
- repository role: reference and community MCP server implementations;
- archived: false.

## Primary evidence packet

The repository demonstrates typed external tool/resource contracts and adapters for filesystem, source-control and service access. It is useful for Ptah facility adapters and application interoperability.

## Independent verification packet

Each server, external API, credential, filesystem root and tool has an independent authority and risk boundary. MCP hints and schemas are adapter metadata, not Ptah Policy or canonical object/activity identity. A successful tool response does not prove external post-conditions.

## Contradiction and supersession

MCP remains one adapter protocol, not Ptah's internal model or authority system.

## Bounded outcome

`accepted_for_archive_mit_external_tool_adapter_catalogue_with_server_credential_and_authority_boundaries`

Allowed reuse:

- study or wrap pinned MCP servers as replaceable application/facility adapters;
- retain exact server, schema, tool, resource, credential reference and result evidence.

Restrictions:

- review each server and dependency separately;
- grant only caller-configured roots, resources and credentials;
- do not map MCP tool/resource IDs to canonical Ptah identities or Policy;
- do not treat tool success as result acceptance or external-effect proof.

This outcome does not authorize implementation.