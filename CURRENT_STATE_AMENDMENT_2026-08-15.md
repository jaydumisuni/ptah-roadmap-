# Ptah Current-State Amendment — 2026-08-15

**Status:** OPERATIVE / MERGED AUTHORITY  
**Applies to:** `CURRENT_STATE.md`, P01 host qualification and proposed ADR-0033  
**Authority:** owner-directed architecture correction recorded in accepted ADR-0038

## Why this amendment exists

`CURRENT_STATE.md` still preserves the historical P01 wording as an exact physical Ubuntu Server 24.04.4 / `6.8.0-136-generic` host proof. That no longer matches the accepted Prime/Ptah deployment boundary.

Prime owns machine authority and Ptah remains an independent OS-neutral mechanical substrate. Ptah will be integrated with Prime through capability projection rather than hosted in a second operating system solely for Ptah.

This amendment is intentionally separate so the historical P01 evidence chain is not rewritten or falsified.

## Operative current position

- Ptah architecture: accepted planning baseline.
- Active phase: Phase 0C.
- Active work unit: **P01D — Kratos physical development-host qualification**.
- P01D tooling state: **PUBLIC PROBE MERGED / READY FOR PHYSICAL EXECUTION**.
- Selected physical development machine: **Kratos**.
- Kratos operating system: **use the current installed system; no replacement required for P01D**.
- Ptah-specific bootable/server OS: **not required**.
- Dedicated guest VM/server solely for Ptah: **not required**.
- Current private control path: **Oracle MCP/RPC**.
- Public proof tooling: provider-neutral; it exposes no Oracle, Kratos, Prime or private THETECHGUY topology.
- Public probe claim: portable capability behavior + exact repository binding only; it does **not** establish physical-machine identity or accept the host.
- Physical-machine identity proof: retained Oracle MCP/RPC execution receipt reviewed privately with the public report.
- Runtime implementation: **NOT AUTHORIZED until the combined P01D evidence is independently accepted**.
- Later Prime-native integration proof: **P01P — OPEN / DEFERRED; it does not block starting runtime implementation after P01D closes**.

## Frozen P01D public proof checkpoint

The provider-neutral public proof tooling is merged into `jaydumisuni/Ptah-space` at:

```text
d9474acaaa01bce27c0b34c951a5dc7faec75081
```

The public physical-proof tracking issue is:

```text
Ptah-space #20 — Run Phase 0C physical development-host qualification
```

Before merge, the exact candidate head passed the complete Ptah-space workflow set. The dedicated development-host proof workflow passed on both Windows 2025 and Ubuntu 24.04 hosted runners. Hosted CI proves cross-platform probe behavior only and cannot satisfy the Kratos physical-machine binding requirement.

## What P01D must prove

The physical Kratos run must execute the exact merged public probe and produce a report showing the portable first-runtime prerequisites defined by ADR-0038 and `host/development-host-contract.json`, including:

- process execution;
- temporary Workspace behavior;
- file durability call;
- atomic replacement;
- advisory locking;
- monotonic time;
- local stream sockets;
- threading;
- resource observation;
- exact clean repository binding;
- retained PASS/FAIL evidence.

The public report must have:

```text
portable_capabilities_passed: true
physical_host_identity_verified: false
development_host_accepted: false
runtime_implementation_authorized: false
deployment_host_qualified: false
release_accepted: false
```

Capability, observation, repository-binding and aggregate probe failure sets must be empty.

The false acceptance/authorization fields are mandatory. The public probe cannot accept itself.

The private Oracle MCP/RPC receipt must establish that this exact report was generated by execution on Kratos. Independent review combines those two evidence layers.

Distribution/kernel identity is observed but is not a P01D acceptance predicate.

## Exact physical execution order

1. Through Oracle MCP/RPC, identify/confirm the selected Kratos execution target.
2. On Kratos, recover an exact clean checkout of Ptah-space commit `d9474acaaa01bce27c0b34c951a5dc7faec75081`.
3. Confirm the checkout is clean before collection.
4. Choose a proof output path outside the repository checkout.
5. Execute `tools/run_development_host_probe.py` with the exact commit and `--require-portable-pass` according to the merged runbook.
6. Retain the resulting public report outside the checkout.
7. Retain the Oracle MCP/RPC request/response/execution receipt separately in private evidence.
8. Preserve any failed or partial attempt; do not overwrite it with a retry.
9. Independently review the public report and private machine-binding receipt together.
10. Only if every P01D acceptance condition passes, record final P01D acceptance and explicit A01 runtime authorization.
11. Keep P01P open/deferred.

Do not silently substitute hosted CI, another physical machine, an ad-hoc shell or another control transport for the approved proof path.

## What moved to P01P

Prime-owned machine mechanisms are verified later against Prime's Capability Interface and Workload Policy, including:

- process/PTY substrate;
- resource accounting/enforcement;
- isolation;
- secure IPC/capability exposure;
- storage semantics and change observation;
- container/VM/remote execution backends when selected;
- recovery/restart behavior;
- Prime Host -> Ptah Node/Provider/Facility projection;
- explicit `Prime Host ID != Ptah Node ID` identity separation.

Linux implementation details such as cgroups, namespaces, seccomp, overlayfs and AppArmor remain useful lower-layer evidence when Prime uses them, but Ptah is not bound to an Ubuntu distribution identity.

## Superseded interpretation

The following interpretation is no longer operative for runtime-start authorization:

> Ptah must first install/boot an exact Ubuntu Server 24.04.4 host with kernel `6.8.0-136-generic` and close the exact APT pinned-host bundle before runtime implementation may begin.

The old tooling and records remain historical evidence. They must not be relabelled as passed.

Ptah-space issue #17 was closed as superseded/not-planned. That closure is not evidence that the old proof passed.

## Completed synchronization evidence

Architecture correction:

```text
ptah-roadmap- merge: 96d2b7d70f07b801982f215247ab07e8e750568b
```

Public proof tooling:

```text
Ptah-space merge: d9474acaaa01bce27c0b34c951a5dc7faec75081
```

Ecosystem recovery synchronization:

```text
TTG-progress merge: d8b03723c7c9cd02c347c681240a4295367cb86f
TTG-ecosystem merge: 638a3ffa5afb3b748ec10fca10caf86e50304b1c
TTG-decisions merge: 3f5d3369e8e1f669f3799f4a0b5c14cd70e008c7
```

These synchronization merges do not constitute P01D physical evidence.

## Resume order

1. Recover this operative amendment and ADR-0038.
2. Confirm Ptah-space public proof commit `d9474acaaa01bce27c0b34c951a5dc7faec75081` remains the selected physical-proof checkpoint.
3. Use Oracle MCP/RPC to execute that exact probe on Kratos's current system.
4. Retain the public probe report plus Oracle MCP/RPC execution receipt privately as one P01D evidence set.
5. Independently review portable results, exact repository binding, physical-machine receipt and any negative attempts.
6. Record P01D closure and explicit A01 runtime authorization only if the combined evidence passes.
7. Begin A01 implementation only after that authorization exists.
8. Keep P01P open until Prime exposes the required machine Capability Interface for final integration qualification.

## Non-effects

This amendment does not:

- accept P01D;
- authorize runtime implementation by itself;
- merge Ptah semantics into Prime Core;
- change WP01-WP14 contracts;
- alter accepted licence/source/backend boundaries;
- claim Prime OS is already implemented;
- claim the old Ubuntu pinned-host proof passed;
- allow hosted CI to masquerade as the Kratos physical proof;
- weaken Ptah evidence or independent-review requirements.
