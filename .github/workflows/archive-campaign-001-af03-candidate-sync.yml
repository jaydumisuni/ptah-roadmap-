name: Archive Campaign 001 AF03 Candidate Sync

on:
  pull_request:
    paths:
      - "tools/apply_af03_candidate_recovery_sync.py"
      - "tools/apply_af03_candidate_recovery_sync_v2.py"
      - ".github/workflows/archive-campaign-001-af03-candidate-sync.yml"

permissions:
  contents: write

env:
  PTAH_HEAD_REF: ${{ github.event.pull_request.head.ref }}
  PTAH_EXACT_SHA: ${{ github.event.pull_request.head.sha }}

jobs:
  synchronize-af03-candidate:
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Check out exact AF03 branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ env.PTAH_HEAD_REF }}
          persist-credentials: true
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"

      - name: Verify exact starting head
        run: test "$(git rev-parse HEAD)" = "$PTAH_EXACT_SHA"

      - name: Apply guarded recovery synchronization
        run: |
          mkdir -p conformance/reports/af03-candidate-sync
          set -o pipefail
          python3 tools/apply_af03_candidate_recovery_sync_v2.py 2>&1 \
            | tee conformance/reports/af03-candidate-sync/transform.txt

      - name: Run candidate and governing validations
        run: |
          set -o pipefail
          python3 tools/test_check_archive_af03.py -v 2>&1 | tee conformance/reports/af03-candidate-sync/af03-tests.txt
          python3 tools/check_archive_af03.py --repo-root . 2>&1 | tee conformance/reports/af03-candidate-sync/af03-validator.txt
          python3 tools/test_check_archive_af02.py -v 2>&1 | tee conformance/reports/af03-candidate-sync/af02-tests.txt
          python3 tools/check_archive_af02.py --repo-root . 2>&1 | tee conformance/reports/af03-candidate-sync/af02-validator.txt
          python3 tools/test_check_archive_formation.py -v 2>&1 | tee conformance/reports/af03-candidate-sync/archive-tests.txt
          python3 tools/check_archive_formation.py --repo-root . 2>&1 | tee conformance/reports/af03-candidate-sync/archive-validator.txt
          python3 tools/test_check_platform_diagnostic_advisory.py -v 2>&1 | tee conformance/reports/af03-candidate-sync/diagnostic-tests.txt
          python3 tools/check_platform_diagnostic_advisory.py --repo-root . 2>&1 | tee conformance/reports/af03-candidate-sync/diagnostic-validator.txt
          python3 tools/test_check_master_plan_closure.py -v 2>&1 | tee conformance/reports/af03-candidate-sync/master-plan-tests.txt
          python3 tools/check_master_plan_closure.py --repo-root . 2>&1 | tee conformance/reports/af03-candidate-sync/master-plan-validator.txt

      - name: Upload synchronization diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: af03-candidate-sync-${{ env.PTAH_EXACT_SHA }}-${{ github.run_attempt }}
          path: conformance/reports/af03-candidate-sync/
          if-no-files-found: error
          retention-days: 30

      - name: Verify changed-file boundary
        run: |
          expected=$(cat <<'EOF'
          AI_HANDOFF.md
          CURRENT_STATE.md
          PROGRESS.md
          archive/CAMPAIGN-001-FORMATION-MANIFEST.md
          master-plan-index.json
          EOF
          )
          actual=$(git diff --name-only | sort)
          test "$actual" = "$(printf '%s\n' "$expected" | sort)"
          grep -Fq 'AF03 candidate package: COMPLETE / SERGEANT PASSED / NOT ACCEPTED' archive/CAMPAIGN-001-FORMATION-MANIFEST.md
          grep -Fq -- '- AF03: READY / NOT STARTED;' CURRENT_STATE.md
          grep -Fq '**Runtime implementation:** NOT AUTHORIZED' CURRENT_STATE.md
          ! grep -Fq '**Runtime implementation:** AUTHORIZED' CURRENT_STATE.md
          grep -Fq 'Status: proposed' decisions/ADR-0033-FIRST-VERTICAL-SLICE-HOST-LICENCE-LAYOUT-BACKENDS.md
          grep -Fq '**Status:** COMPLETE AND FROZEN' DONOR_RECOVERY.md

      - name: Commit synchronized recovery records
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            AI_HANDOFF.md \
            CURRENT_STATE.md \
            PROGRESS.md \
            archive/CAMPAIGN-001-FORMATION-MANIFEST.md \
            master-plan-index.json
          git commit -m "Synchronize AF03 candidate recovery state"
          git push origin "HEAD:${PTAH_HEAD_REF}"
