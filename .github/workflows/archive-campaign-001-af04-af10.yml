name: Archive Campaign 001 Complete

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  PTAH_EXACT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}

jobs:
  archive-campaign-complete:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Check out exact campaign head
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ env.PTAH_EXACT_SHA }}
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"

      - name: Verify exact checked-out head
        run: test "$(git rev-parse HEAD)" = "$PTAH_EXACT_SHA"

      - name: Run permanent campaign regressions
        run: |
          mkdir -p conformance/reports/archive-campaign-001-complete
          set -o pipefail
          python3 tools/test_check_archive_campaign_complete.py -v 2>&1 \
            | tee conformance/reports/archive-campaign-001-complete/regressions.txt

      - name: Validate durably bound campaign
        run: |
          set -o pipefail
          python3 tools/check_archive_campaign_complete.py \
            --repo-root . \
            --output conformance/reports/archive-campaign-001-complete/validation.json \
            2>&1 | tee conformance/reports/archive-campaign-001-complete/validator-output.txt

      - name: Bind permanent evidence to exact head
        run: |
          python3 - <<'PY'
          import hashlib, json, os, subprocess
          from datetime import datetime, timezone
          from pathlib import Path
          root = Path("conformance/reports/archive-campaign-001-complete")
          report_path = root / "validation.json"
          report = json.loads(report_path.read_text(encoding="utf-8"))
          expected = os.environ["PTAH_EXACT_SHA"]
          checked = subprocess.run(["git","rev-parse","HEAD"],capture_output=True,text=True,check=True).stdout.strip()
          evidence = {
              "schema_version":"1.0.0",
              "record_type":"ptah.archive_campaign_001_complete_exact_head_evidence",
              "created_at":datetime.now(timezone.utc).isoformat(timespec="seconds"),
              "implementation_commit":expected,
              "checked_out_commit":checked,
              "exact_head_match":checked == expected,
              "validation_report_sha256":hashlib.sha256(report_path.read_bytes()).hexdigest(),
              **report,
          }
          (root / "exact-head-evidence.json").write_text(json.dumps(evidence,indent=2)+"\n",encoding="utf-8")
          if not evidence["exact_head_match"]:
              raise SystemExit("campaign completion did not test exact head")
          if evidence["status"] != "campaign_001_complete_bound_non_authorizing":
              raise SystemExit("campaign completion status invalid")
          expected_values = {
              "formation_count":10,
              "obligation_count":98,
              "accepted_archive_record_count":91,
              "blocked_completed_outcome_count":7,
              "remaining_evidence_count":0,
          }
          for key, value in expected_values.items():
              if evidence.get(key) != value:
                  raise SystemExit(f"campaign completion evidence mismatch: {key}")
          for key in ("phase_0a_reopened","p01_closed","adr_0033_accepted","runtime_implementation_authorized"):
              if evidence.get(key) is not False:
                  raise SystemExit(f"campaign authority boundary violated: {key}")
          PY

      - name: Upload exact-head campaign evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: archive-campaign-001-complete-${{ env.PTAH_EXACT_SHA }}-${{ github.run_attempt }}
          path: conformance/reports/archive-campaign-001-complete/
          if-no-files-found: error
          retention-days: 30
